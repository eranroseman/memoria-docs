---
mode: how-to
audience: operator
topic: plugins
---

# obsidian-kanban

Used for `board-state` rendering when the human prefers a Kanban view over the default Dataview tables.

Load-bearing settings: none for Memoria specifically. Default settings are fine. If used, the dashboard needs the `kanban-plugin: board` frontmatter key in its markdown.