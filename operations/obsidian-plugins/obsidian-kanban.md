# obsidian-kanban

Used for `board-state` rendering when the operator prefers a Kanban view over the default Dataview tables.

Load-bearing settings: none load-bearing for Memoria specifically. Default settings are fine. If used, the dashboard needs the `kanban-plugin: board` frontmatter key in its markdown.
